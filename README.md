# پروژه تحلیل و یادگیری ماشین آگهی‌های املاک دیوار

پروژه گروهی بوت‌کمپ هوش مصنوعی کوئرا برای تحلیل آماری، آزمون فرض، خوشه‌بندی و مدل‌سازی روی دیتاست آگهی‌های املاک دیوار.

## اعضای تیم

* علی
* بنیامین
* لیلا
* تینا

## ساختار پروژه

```text
divar-ml-project/
├── README.md
├── .gitignore
├── requirements.txt
├── utils.py
└── notebooks/
    ├── 01_preprocessing.ipynb
    ├── 02_descriptive_stats.ipynb
    ├── 03_hypothesis_tests.ipynb
    ├── 04_clustering.ipynb
    ├── 05_prediction.ipynb
    └── work/
```

توضیح فایل‌ها:

* `README.md`: نقشه کلی پروژه، مسیر فایل‌ها، قوانین تیمی و وضعیت خروجی‌ها
* `.gitignore`: جلوگیری از آپلود فایل‌های دیتا و فایل‌های موقت
* `requirements.txt`: کتابخانه‌های موردنیاز پروژه
* `utils.py`: توابع مشترک پروژه مانند تبدیل تاریخ، تبدیل UTM و توابع کمکی قیمت
* `01_preprocessing.ipynb`: نوت‌بوک اصلی preprocessing و ادغام کارهای اعضای تیم
* `02_descriptive_stats.ipynb`: آمار توصیفی
* `03_hypothesis_tests.ipynb`: آزمون فرض
* `04_clustering.ipynb`: خوشه‌بندی و توصیه‌گر
* `05_prediction.ipynb`: پیش‌بینی قیمت
* `notebooks/work/`: نوت‌بوک‌های کاری هر نفر قبل از ادغام

## دسترسی به داده‌ها

داده‌ها روی Google Drive نگه‌داری می‌شوند و نباید داخل GitHub آپلود شوند.

### پوشه دیتای خام

```text
/content/drive/MyDrive/Divar Dataset/
```

فایل‌های اصلی:

```text
Divar.csv
iran_city_classification.csv
```

### پوشه خروجی‌های تیم

```text
/content/drive/MyDrive/divar_project/
```

فایل‌های خروجی preprocessing:

```text
cleaned_step1.parquet
cleaned_step2_benyamin_v2.parquet
cleaned_step3_geo.parquet
```

برای لود دیتای خام:

```python
df = pd.read_csv("/content/drive/MyDrive/Divar Dataset/Divar.csv")
```

برای لود آخرین نسخه تمیزشده:

```python
df = pd.read_parquet("/content/drive/MyDrive/divar_project/cleaned_step3_geo.parquet")
```

برای لود خروجی‌های میانی preprocessing:

```python
df_step1 = pd.read_parquet("/content/drive/MyDrive/divar_project/cleaned_step1.parquet")
df_step2 = pd.read_parquet("/content/drive/MyDrive/divar_project/cleaned_step2_benyamin_v2.parquet")
df_step3 = pd.read_parquet("/content/drive/MyDrive/divar_project/cleaned_step3_geo.parquet")
```

نکته: خروجی‌های تمیزشده با فرمت `parquet` ذخیره شده‌اند تا نوع داده‌ها مثل `category`، `boolean`، `datetime` و `Int64` بهتر حفظ شوند.

## محیط اجرا

* اجرای کدها: Google Colab
* نگه‌داری کدها: GitHub
* نگه‌داری داده‌ها: Google Drive

برای نصب کتابخانه‌های موردنیاز:

```python
!pip install -r requirements.txt
```

در صورتی که نوت‌بوک مستقیم در Colab اجرا شود و فایل `requirements.txt` در همان مسیر در دسترس نباشد، کتابخانه‌های اصلی می‌توانند جداگانه نصب شوند:

```python
!pip install -q jdatetime utm pyarrow
```

## نکات مهم درباره داده

* ستون‌های `transformable_price` و `rent_credit_transform` با وجود نامشان، مقدار عددی قیمت نیستند و به‌عنوان flagهای `True/False` استفاده می‌شوند.
* قیمت فروش در ستون `price_value` قرار دارد.
* مقدار رهن در ستون `credit_value` قرار دارد.
* مقدار اجاره در ستون `rent_value` قرار دارد.
* بعضی از مقدارهای گم‌شده ساختاری هستند و نباید با مقدار کلی پر شوند.
* مقدارهای گم‌شده‌ی `construction_year` در بعضی دسته‌ها ساختاری هستند؛ بنابراین ستون اصلی `construction_year` با میانه پر نشده و مقدارهای خالی آن حفظ شده‌اند.
* برای استفاده احتمالی در مدل‌سازی، دو ستون کمکی `construction_year_was_missing` و `construction_year_imputed` ساخته شده‌اند.
* ستون‌هایی مثل `land_size`، `floor`، `total_floors_count`، `unit_per_floor`، `regular_person_capacity` و ستون‌های `transformed_*` در بسیاری از ردیف‌ها عمداً خالی باقی مانده‌اند، چون معنی آن‌ها به نوع آگهی وابسته است.
* برای تحلیل قیمت، ردیف‌ها حذف نشده‌اند؛ به‌جای حذف، flagهای اعتبارسنجی ساخته شده‌اند.
* برای تحلیل جغرافیایی نیز ردیف‌ها حذف نشده‌اند؛ فقط ردیف‌های قابل استفاده با flag مشخص شده‌اند.

## خروجی مرحله اول preprocessing

در مرحله اول، دیتای خام خوانده شد و عملیات پایه preprocessing انجام شد.

کارهای اصلی این مرحله:

* حذف ستون اضافی `Unnamed: 0`
* اصلاح نوع داده ستون‌های عددی
* تبدیل ستون‌های بولین به نوع مناسب
* تبدیل مقدار `unselect` به `NaN`
* تبدیل تاریخ میلادی `created_at_month` به تاریخ شمسی
* ساخت ستون‌های `created_at_shamsi` و `created_at_shamsi_readable`
* حذف چند ستون غیرضروری مربوط به اجاره روزانه
* حفظ مقدارهای گم‌شده‌ی `construction_year` در ستون اصلی
* ساخت ستون `construction_year_was_missing` برای مشخص کردن ردیف‌هایی که سال ساخت نداشتند
* ساخت ستون `construction_year_imputed` برای نسخه‌ی پرشده‌ی سال ساخت با میانه کل
* پر کردن مقدارهای گم‌شده `rooms_count` و `building_size` با میانه هر `cat3_slug`
* حذف ردیف‌هایی که ستون‌های کلیدی `title`، `cat3_slug` یا `city_slug` را ندارند

خروجی این مرحله:

```text
cleaned_step1.parquet
```

خلاصه خروجی:

```text
total_rows: 999,943
output_columns_count: 55
dropped_rows: 57
missing_construction_year: 184,164
missing_construction_year_imputed: 0
```

## خروجی مرحله دوم preprocessing

در مرحله دوم، کدهای بنیامین روی خروجی مرحله اول اجرا شد.

در این مرحله ستون‌های مربوط به قیمت، متراژ، قیمت هدف و flagهای اعتبارسنجی ساخته شدند. هیچ ردیفی در این مرحله حذف نشده است.

ستون‌های مهم ساخته‌شده:

* `transaction_type`: نوع معامله شامل فروش، اجاره، اجاره موقت یا سایر موارد
* `price_value_pos`: نسخه مثبت قیمت فروش
* `rent_value_pos`: نسخه مثبت اجاره
* `credit_value_pos`: نسخه مثبت رهن
* `building_size_pos`: نسخه مثبت متراژ بنا
* `land_size_pos`: نسخه مثبت متراژ زمین
* `area_for_unit_price`: متراژ مبنا برای محاسبه قیمت واحد
* `monthly_rent_equivalent`: معادل اجاره ماهانه برای آگهی‌های اجاره
* `credit_equivalent`: معادل رهن برای آگهی‌های اجاره
* `sale_price_per_m2`: قیمت فروش به ازای هر متر مربع
* `monthly_rent_equivalent_per_m2`: اجاره ماهانه معادل به ازای هر متر مربع
* `credit_equivalent_per_m2`: رهن معادل به ازای هر متر مربع
* `target_price`: قیمت هدف برای تحلیل و مدل‌سازی
* `target_price_type`: نوع قیمت هدف
* `target_price_per_m2`: قیمت هدف به ازای هر متر مربع
* `is_valid_for_price_analysis`: flag نهایی برای انتخاب ردیف‌های مناسب تحلیل قیمت
* `has_geo`: مشخص می‌کند ردیف مختصات جغرافیایی دارد یا نه
* `is_valid_geo_basic`: اعتبارسنجی اولیه مختصات جغرافیایی

خروجی این مرحله:

```text
cleaned_step2_benyamin_v2.parquet
```

خلاصه خروجی:

```text
total_rows: 999,943
rows_with_target_price: 917,334
valid_for_price_analysis: 877,587
invalid_for_price_analysis: 122,356
rows_with_geo: 655,594
invalid_geo_rows: 18
output_columns_count: 80
```

## خروجی مرحله سوم preprocessing

در مرحله سوم، پردازش جغرافیایی داده‌ها توسط علی انجام شد.

در این مرحله ردیف‌ها حذف نشدند. ابتدا مختصات جغرافیایی بررسی شد، سپس برای ردیف‌هایی که مختصات معتبر داشتند، تبدیل latitude و longitude به UTM انجام شد.

ستون‌های مهم ساخته‌شده:

* `is_valid_geo_for_analysis`: flag نهایی برای مشخص کردن ردیف‌های قابل استفاده در تحلیل جغرافیایی
* `utm_easting`: مختصات UTM در محور شرقی-غربی
* `utm_northing`: مختصات UTM در محور شمالی-جنوبی
* `utm_zone_number`: شماره zone در سیستم UTM
* `utm_zone_letter`: حرف zone در سیستم UTM

منطق ستون `is_valid_geo_for_analysis`:

* مقدار `True`: ردیف مختصات معتبر دارد و برای تحلیل جغرافیایی قابل استفاده است.
* مقدار `False`: ردیف مختصات ندارد یا مختصات آن نامعتبر است.

خروجی این مرحله:

```text
cleaned_step3_geo.parquet
```

خلاصه خروجی:

```text
total_rows: 999,943
valid_geo_for_analysis: 655,576
invalid_or_missing_geo_for_analysis: 344,367
invalid_geo_rows: 18
output_columns_count: 85
```

بررسی zoneهای UTM نشان داد داده‌ها در چند zone مختلف قرار دارند. بیشترین حجم داده در zone زیر است:

```text
UTM zone 39S: 452,153 rows ≈ 68.97%
```

نکته: چون داده‌ها در چند zone مختلف UTM قرار دارند، در مراحل بعدی، مخصوصاً clustering سراسری، نباید `utm_easting` و `utm_northing` بدون توجه به `utm_zone_number` و `utm_zone_letter` مستقیم با هم مقایسه شوند.

## خروجی نهایی preprocessing

آخرین فایل آماده برای استفاده در مراحل بعدی پروژه:

```text
cleaned_step3_geo.parquet
```

مشخصات فایل نهایی:

```text
rows: 999,943
columns: 85
rows_with_target_price: 917,334
valid_for_price_analysis: 877,587
valid_geo_for_analysis: 655,576
missing_construction_year: 184,164
```

## وضعیت آمار توصیفی

بخشی از آمار توصیفی در نوت‌بوک کاری مربوط به علی انجام شده و بعداً باید در نوت‌بوک اصلی `02_descriptive_stats.ipynb` ادغام شود.

### سؤال ۱: توزیع آگهی‌ها در دسته‌بندی‌ها

در این بخش، توزیع آگهی‌ها بر اساس `cat2_slug` و `cat3_slug` بررسی شد.

خلاصه نتایج:

```text
cat2_slug unique values: 6
cat3_slug unique values: 16
```

در دسته‌بندی سطح دو، بیشترین سهم مربوط به:

```text
residential-sell: 558,692 rows ≈ 55.87%
residential-rent: 276,528 rows ≈ 27.65%
```

در دسته‌بندی سطح سه، بیشترین سهم مربوط به:

```text
apartment-sell: 303,372 rows ≈ 30.34%
apartment-rent: 211,853 rows ≈ 21.19%
plot-old: 133,570 rows ≈ 13.36%
house-villa-sell: 121,750 rows ≈ 12.18%
```

نتیجه: دیتاست از نظر دسته‌بندی نامتوازن است و بخش عمده آن مربوط به آگهی‌های مسکونی، مخصوصاً آپارتمان فروش و آپارتمان اجاره است.

### سؤال ۲: هیستوگرام سال ساخت

در این بخش، توزیع `construction_year` بررسی شد.

نکته مهم: چون مقدارهای گم‌شده‌ی `construction_year` در برخی دسته‌ها ساختاری هستند، برای رسم هیستوگرام از ستون اصلی `construction_year` استفاده شد و مقدارهای گم‌شده در نمودار پر نشدند.

خلاصه نتایج:

```text
most_frequent_construction_year: 1403
share_of_1403_among_valid_years: 14.25%
```

سال `1390` نیز فراوانی نسبتاً بالایی داشت. بررسی تکمیلی نشان داد این مقدار فقط در یک دسته خاص متمرکز نیست و در چند دسته اصلی مانند `apartment-rent`، `apartment-sell`، `house-villa-rent` و `house-villa-sell` دیده می‌شود.

نتیجه: بخش قابل توجهی از آگهی‌ها مربوط به املاک نوساز یا نسبتاً جدید است. همچنین جهش سال `1390` می‌تواند ترکیبی از واقعیت بازار و ورود تقریبی سال ساخت توسط کاربران باشد.

### سؤال ۳: تعداد آگهی‌های فروش و اجاره در ماه‌های مختلف

در این بخش، تعداد آگهی‌های `sell` و `rent` در ماه‌های مختلف شمسی بررسی شد.

ابتدا همه ماه‌های موجود در دیتاست رسم شدند. نمودار کامل نشان داد پوشش زمانی داده‌ها یکنواخت نیست و بخش اصلی داده‌ها در بازه زیر قرار دارد:

```text
1403-02 تا 1403-09
```

برای اطمینان از اینکه این موضوع ناشی از preprocessing نیست، تعداد آگهی‌ها در دیتای خام و دیتای نهایی مقایسه شد.

```text
raw_rows: 1,000,000
final_rows: 999,943
dropped_rows: 57
```

نتیجه: کم بودن تعداد آگهی‌ها در ماه‌های خارج از بازه اصلی از خود دیتای خام ناشی شده و مربوط به preprocessing نیست.

در بازه قابل اتکا، آگهی‌های فروش نسبتاً پایدارتر بودند و بیشتر بین حدود `67` هزار تا `77` هزار آگهی در ماه قرار داشتند. آگهی‌های اجاره نوسان بیشتری داشتند.

افزایش‌های مهم:

```text
rent in 1403-03: +49.51% compared to previous month
sell in 1403-07: +11.08% compared to previous month
```

نتیجه: افزایش چشم‌گیر اصلی مربوط به آگهی‌های اجاره در `1403-03` است. برای فروش نیز در `1403-07` افزایش قابل توجهی دیده می‌شود، اما شدت آن کمتر از جهش اجاره است.

### سؤال ۶: ترند میانگین قیمت اجاره

این سؤال موقتاً شروع شد، اما هنوز کامل نشده و مسئولیت تکمیل نهایی آن با بنیامین است.

نکته‌های بررسی‌شده تا این مرحله:

* ستون مناسب برای تحلیل اجاره، `monthly_rent_equivalent` در نظر گرفته شد.
* دلیل انتخاب این ستون این است که اثر رهن را به معادل اجاره ماهانه تبدیل می‌کند.
* داده اجاره معتبر با فیلترهای زیر ساخته شد:

```text
transaction_type == "rent"
target_price_type == "monthly_rent_equivalent"
is_valid_for_price_analysis == True
monthly_rent_equivalent > 0
```

خروجی اولیه:

```text
Rent data shape: (339272, 85)
```

بازه قابل اتکا دوباره به شکل زیر تشخیص داده شد:

```text
1403-02 تا 1403-09
```

پرت‌ها با صدک‌های ۱٪ و ۹۹٪ بررسی شدند:

```text
lower bound: 1,111,111
upper bound: 210,000,000
```

بعد از فیلتر پرت‌ها و انتخاب بازه قابل اتکا:

```text
rows after filtering: 331,205
```

وضعیت نهایی: این سؤال فعلاً کامل نشده است و باید توسط بنیامین تکمیل شود.

## قوانین کار تیمی

* هر نفر فقط روی نوت‌بوک مربوط به فاز خودش کار می‌کند.
* همزمان دو نفر روی یک فایل مشترک کار نمی‌کنند.
* قبل از شروع کار، آخرین نسخه فایل‌ها از GitHub گرفته می‌شود.
* بعد از اتمام هر بخش، commit با پیام واضح ثبت می‌شود.
* commitهای کوچک و مکرر بهتر از یک commit بزرگ و مبهم هستند.
* فایل‌های دیتا نباید داخل GitHub آپلود شوند.
* نام فایل‌ها و مسیرها نباید فاصله داشته باشند.
* خروجی‌های سنگین فقط در Google Drive نگه‌داری می‌شوند.
* برای بخش یادگیری ماشین، علی و بنیامین باید setup، load data، فیلتر پایه و sample مشترک داشته باشند تا خروجی‌های مسئله ۱ با هم سازگار باشند.

## تقسیم وظایف

### آماده‌سازی داده

* مدیریت مقادیر گم‌شده: علی ✅
* اصلاح نوع داده‌ها: علی ✅
* تبدیل تاریخ شمسی: علی ✅
* ساخت خروجی مرحله اول preprocessing: علی ✅
* ساخت ستون قیمت واحد: بنیامین ✅
* ساخت قیمت هدف و معادل‌سازی رهن/اجاره: بنیامین ✅
* مدیریت داده‌های پرت قیمت و متراژ با flag: بنیامین ✅
* ساخت خروجی مرحله دوم preprocessing: بنیامین ✅
* بررسی و آماده‌سازی داده‌های جغرافیایی: علی ✅
* تبدیل مختصات latitude/longitude به UTM: علی ✅
* ساخت خروجی مرحله سوم preprocessing: علی ✅
* ادغام نهایی در نوت‌بوک اصلی `01_preprocessing.ipynb`: علی ✅
* جمع‌بندی و مستندسازی دیتا: تینا

### آمار توصیفی

* سؤال ۱، ۲، ۳: علی ✅
* سؤال ۴، ۵، ۶: بنیامین
* سؤال ۷: لیلا
* سؤال ۸، ۹: تینا

### آزمون فرض

* فرضیه ۱: علی ✅

  * موضوع: بررسی کمتر بودن میانگین مساحت خانه‌های مسکونی در کلان‌شهرها نسبت به شهرهای کوچک
  * روش: Welch t-test یک‌طرفه
  * نتیجه: فرض صفر رد شد؛ میانگین متراژ در کلان‌شهرها از نظر آماری کمتر است.
  * نکته: اختلاف میانگین حدود `1.81` متر مربع و اندازه اثر بسیار کوچک است، بنابراین تفاوت از نظر عملی چندان بزرگ نیست.

* فرضیه ۲: بنیامین ✅

  * موضوع: بررسی این ادعا که «قدیما خانه‌ها دلبازتر بود»؛ یعنی آیا میانگین مساحت خانه‌های قدیمی‌ساخت بیشتر از خانه‌های جدیدساخت است یا نه.
  * تعریف خانه قدیمی‌ساخت: خانه‌هایی با `construction_year < 1396`
  * تعریف خانه جدیدساخت: خانه‌هایی با `construction_year >= 1396`
  * متغیر مورد بررسی: `building_size_pos`
  * داده مورد استفاده: فقط آگهی‌های مسکونی شامل `apartment-sell`، `apartment-rent`، `house-villa-sell` و `house-villa-rent`
  * فیلترهای اعتبارسنجی:
    * `is_valid_area_basic == True`
    * `is_valid_area_q01_q99_by_cat3 == True`
  * روش: Welch t-test یک‌طرفه
  * فرض صفر: `H0: μ_old ≤ μ_new`
  * فرض مقابل: `H1: μ_old > μ_new`

  خلاصه خروجی:

  ```text
  old_count: 346,748
  new_count: 342,345
  old_mean_area: 107.35
  new_mean_area: 125.18
  mean_difference_old_minus_new: -17.84
  t_statistic: -74.87
  p_value: 1.00
  alpha: 0.05
* فرضیه ۳: لیلا
* فرضیه ۴: تینا

## یادگیری ماشین

### اولویت کلی بخش ML

در بخش یادگیری ماشین، اولویت اصلی تیم تکمیل کامل `مسئله ۱: ساخت سیستم توصیه‌گر` است.

این مسئله شامل سه بخش اصلی است:

```text
مقدمه مسئله ۱: انتخاب و توجیه ویژگی‌های مهم برای خوشه‌بندی
بخش ۱: اجرای KMeans با ۱۰ خوشه و رسم scatter plot و مرکز خوشه‌ها
بخش ۲: اجرای KMeans برای kهای ۱ تا ۲۰ و انتخاب k مناسب با WCSS
بخش ۳: اجرای DBSCAN با مختصات UTM و قیمت و رسیدن به ۳ کلاستر معنادار
```

تا این مرحله، سهم علی در مسئله ۱ یعنی مقدمه و بخش ۱ کامل شده است. ادامه مسئله ۱ شامل بخش انتخاب `k` با WCSS و اجرای DBSCAN بر عهده بنیامین است.

مسئله ۲ یعنی پیش‌بینی قیمت در اولویت دوم قرار دارد و فقط در صورتی شروع می‌شود که بخش‌های اصلی مسئله ۱ کامل و قابل ارائه شده باشند.

### داده مشترک برای مسئله ۱

برای جلوگیری از ناسازگاری بین خروجی‌های علی و بنیامین، هر دو نفر باید از یک داده مشترک استفاده کنند.

فایل مبنا برای هر دو نفر:

```text
/content/drive/MyDrive/divar_project/cleaned_step3_geo.parquet
```

فیلتر پایه مشترک برای مسئله ۱:

```text
is_valid_for_price_analysis == True
is_valid_geo_for_analysis == True
target_price > 0
utm_zone_number == 39
utm_zone_letter == "S"
```

دلیل محدود کردن تحلیل به `UTM zone 39S` این است که داده‌ها در چند UTM zone مختلف قرار دارند و مقایسه مستقیم `utm_easting` و `utm_northing` بدون کنترل zone از نظر مکانی معتبر نیست.

ویژگی‌های پایه مشترک برای خوشه‌بندی:

```text
utm_easting
utm_northing
log_target_price
```

برای اجرای KMeans در بخش علی، ویژگی‌های زیر نیز اضافه شدند:

```text
building_size_pos
rooms_count
```

برای تکرارپذیری نتایج، مقدار ثابت زیر باید در نمونه‌گیری و مدل‌ها استفاده شود:

```text
random_state = 42
```

در بخش علی، مدل KMeans روی کل داده فیلترشده اجرا شد و از sample فقط برای خواناتر شدن نمودار scatter plot استفاده شد. اگر در بخش بنیامین برای سرعت اجرا از sample استفاده شود، روش نمونه‌گیری باید در نوت‌بوک توضیح داده شود و از `random_state = 42` استفاده شود.

### علی: مسئله ۱، مقدمه و بخش ۱ ✅

فایل کاری:

```text
notebooks/work/clustering_ali.ipynb
```

فایل نهایی برای ادغام:

```text
notebooks/04_clustering.ipynb
```

وضعیت:

```text
کامل شده ✅
```

#### کارهای انجام‌شده در مقدمه مسئله ۱

* توضیح هدف سیستم توصیه‌گر ملک
* توضیح اینکه خوشه‌بندی چگونه می‌تواند برای پیشنهاد آگهی‌های مشابه استفاده شود
* انتخاب و توجیه ویژگی‌های مناسب برای خوشه‌بندی
* توضیح دلیل استفاده از مختصات UTM
* توضیح دلیل محدود کردن تحلیل مکانی به `UTM zone 39S`
* توضیح دلیل استفاده از `log_target_price`
* توضیح نقش `building_size_pos` و `rooms_count`
* توجه به محدود نگه داشتن تعداد ویژگی‌ها برای جلوگیری از نفرین ابعاد

#### کارهای انجام‌شده در بخش ۱

* لود دیتای نهایی preprocessing از `cleaned_step3_geo.parquet`
* بررسی وجود ستون‌های لازم
* اعمال فیلتر پایه مشترک مسئله ۱
* ساخت `log_target_price`
* انتخاب featureهای نهایی برای KMeans
* بررسی مقدارهای گم‌شده در featureهای انتخاب‌شده
* scale کردن featureها با `StandardScaler`
* اجرای `KMeans` با ۱۰ خوشه
* اضافه کردن label خوشه به داده
* ساخت جدول تعداد آگهی‌ها در هر خوشه
* ساخت جدول مراکز خوشه‌ها در مقیاس اصلی
* ساخت جدول خلاصه آماری برای هر خوشه
* بررسی خوشه‌های کوچک با متراژ بسیار بالا
* رسم scatter plot خوشه‌ها روی مختصات UTM
* نمایش مراکز خوشه‌ها روی نمودار
* توضیح نزدیک بودن بعضی مراکز خوشه‌ها در نمودار دوبعدی
* تفسیر نهایی خوشه‌ها برای سیستم توصیه‌گر

#### تنظیمات مدل علی

داده مورد استفاده بعد از فیلتر پایه:

```text
rows_used_for_clustering: 404,253
```

ویژگی‌های نهایی KMeans:

```text
utm_easting
utm_northing
log_target_price
building_size_pos
rooms_count
```

تنظیمات مدل:

```text
KMeans(n_clusters=10, random_state=42, n_init=10)
```

مقدار WCSS / Inertia برای KMeans با ۱۰ خوشه:

```text
525,870.04
```

برای رسم نمودار، مدل روی کل داده اجرا شد؛ اما برای خواناتر شدن scatter plot، از sample فقط برای نمایش استفاده شد:

```text
rows_used_for_model: 404,253
rows_used_for_plot: 80,000
```

#### تعداد آگهی‌ها در خوشه‌های علی

```text
cluster 0: 44,689 rows ≈ 11.05%
cluster 1: 34,859 rows ≈ 8.62%
cluster 2: 38,177 rows ≈ 9.44%
cluster 3: 30,965 rows ≈ 7.66%
cluster 4: 105,825 rows ≈ 26.18%
cluster 5: 437 rows ≈ 0.11%
cluster 6: 2,133 rows ≈ 0.53%
cluster 7: 47,936 rows ≈ 11.86%
cluster 8: 24,155 rows ≈ 5.98%
cluster 9: 75,077 rows ≈ 18.57%
```

#### نکته تحلیلی درباره خوشه‌های متراژ بالا

در خروجی KMeans، خوشه‌های ۵ و ۶ از نظر تعداد کوچک هستند، اما متراژ بسیار بالایی دارند. بررسی ترکیب این خوشه‌ها نشان داد که این بخش‌ها عمدتاً شامل زمین‌های قدیمی، املاک صنعتی، کشاورزی و تجاری هستند.

بنابراین این خوشه‌ها الزاماً خطای مدل یا نویز نیستند؛ بلکه می‌توانند یک سگمنت واقعی و متمایز از بازار املاک را نشان دهند. از دید سیستم توصیه‌گر، جدا شدن این آگهی‌ها منطقی است، چون کاربری که به‌دنبال زمین یا ملک صنعتی بزرگ است، نیاز متفاوتی نسبت به کاربر متقاضی آپارتمان یا ملک معمولی‌تر دارد.

#### نکته تحلیلی درباره نزدیک بودن بعضی مراکز خوشه‌ها

در scatter plot، بعضی از مراکز خوشه‌ها از نظر مکانی نزدیک دیده می‌شوند. دلیل این موضوع این است که نمودار فقط دو بعد مکانی را نمایش می‌دهد:

```text
utm_easting
utm_northing
```

اما مدل KMeans فقط بر اساس موقعیت مکانی خوشه‌بندی نکرده است و با پنج ویژگی آموزش دیده است. بنابراین ممکن است دو خوشه روی نقشه در یک محدوده جغرافیایی نزدیک باشند، اما از نظر سطح قیمت، متراژ یا تعداد اتاق‌ها تفاوت داشته باشند. در چنین حالتی، مرکزهای مکانی آن‌ها نزدیک دیده می‌شود، ولی در فضای کامل ویژگی‌ها دو خوشه جدا محسوب می‌شوند.

این موضوع برای سیستم توصیه‌گر منطقی است؛ چون دو ملک می‌توانند در یک محدوده شهری قرار داشته باشند، اما برای دو نیاز متفاوت کاربر مناسب باشند.

#### خروجی نهایی علی

```text
انتخاب و توجیه ویژگی‌های خوشه‌بندی ✅
داده مشترک آماده برای clustering ✅
KMeans با ۱۰ خوشه ✅
scatter plot خوشه‌ها ✅
مرکز خوشه‌ها روی نمودار ✅
جدول خلاصه خوشه‌ها ✅
بررسی خوشه‌های خاص با متراژ بالا ✅
تفسیر خوشه‌ها برای recommender ✅
```

### بنیامین: مسئله ۱، بخش ۲ و بخش ۳

فایل کاری:

```text
notebooks/work/clustering_benyamin.ipynb
```

فایل نهایی برای ادغام:

```text
notebooks/04_clustering.ipynb
```

بنیامین باید از همان فایل مبنا، همان فیلتر پایه و همان تعریف `log_target_price` استفاده کند تا خروجی او با بخش علی قابل ادغام باشد.

#### مسئولیت بنیامین در بخش ۲

* استفاده از همان داده مشترک علی
* اجرای KMeans برای kهای ۱ تا ۲۰
* محاسبه WCSS برای هر مقدار k
* رسم نمودار elbow
* بررسی واضح بودن یا نبودن elbow
* محاسبه و تحلیل میزان کاهش WCSS در kهای مختلف
* انتخاب مقدار مناسب k با استدلال
* توضیح اینکه چرا k انتخاب‌شده مناسب است
* مقایسه کوتاه k انتخاب‌شده با k برابر ۱۰ که در بخش ۱ استفاده شده است

خروجی مورد انتظار بخش ۲:

```text
نمودار WCSS برای k از ۱ تا ۲۰
تحلیل elbow
k پیشنهادی
مقایسه k پیشنهادی با k = 10
توضیح نهایی انتخاب k
```

#### مسئولیت بنیامین در بخش ۳

* استفاده از همان داده و فیلتر مشترک
* انتخاب ویژگی‌های DBSCAN طبق صورت پروژه
* استفاده از مختصات UTM و قیمت هدف یا نسخه لگاریتمی آن
* scale کردن ویژگی‌ها
* تست چند مقدار برای `eps`
* تست چند مقدار برای `min_samples`
* تنظیم هایپرپارامترها برای رسیدن به ۳ کلاستر معنادار
* رسم scatter plot خروجی DBSCAN
* تحلیل نقاط noise با label برابر `-1`
* توضیح اثر `eps` روی تعداد خوشه‌ها و تعداد noiseها
* توضیح اثر `min_samples` روی سخت‌گیری مدل در تشکیل خوشه
* مقایسه کوتاه خروجی DBSCAN با KMeans

ویژگی‌های پیشنهادی برای DBSCAN:

```text
utm_easting
utm_northing
log_target_price
```

خروجی مورد انتظار بخش ۳:

```text
جدول تست eps و min_samples
خروجی DBSCAN با ۳ کلاستر معنادار
scatter plot نهایی
تحلیل noise
توضیح اثر هایپرپارامترها
مقایسه کوتاه DBSCAN با KMeans
```

### مسئله ۲: پیش‌بینی قیمت

مسئله ۲ در اولویت دوم قرار دارد.

در صورتی که مسئله ۱ کامل و قابل ارائه شود و زمان باقی بماند، علی نسخه اولیه مسئله ۲ را شروع می‌کند.

فایل کاری پیشنهادی:

```text
notebooks/work/prediction_ali_baseline.ipynb
```

فایل نهایی برای ادغام:

```text
notebooks/05_prediction.ipynb
```

محدوده کار علی در مسئله ۲ فقط نسخه اولیه و baseline است، نه مدل‌سازی سنگین.

وظایف علی در صورت داشتن زمان:

* آماده‌سازی داده معتبر برای پیش‌بینی قیمت
* تعریف متغیر هدف
* ساخت نسخه لگاریتمی هدف در صورت نیاز
* حذف featureهایی که باعث نشت متغیر هدف می‌شوند
* انتخاب چند feature اولیه و قابل دفاع
* تقسیم داده به train، validation و test
* آموزش یک baseline ساده
* گزارش اولیه معیارهای ارزیابی
* نوشتن محدودیت‌های مدل اولیه

فیلتر پیشنهادی مسئله ۲:

```text
is_valid_for_price_analysis == True
target_price > 0
```

متغیر هدف پیشنهادی:

```text
log_target_price
```

ستون‌هایی که نباید به عنوان feature استفاده شوند:

```text
target_price
target_price_type
target_price_per_m2
price_value
rent_value
credit_value
price_value_pos
rent_value_pos
credit_value_pos
monthly_rent_equivalent
sale_price_per_m2
monthly_rent_equivalent_per_m2
credit_equivalent
credit_equivalent_per_m2
```

معیارهای گزارش اولیه:

```text
r2_score
MAE
MSE
```

وضعیت مسئله ۲:

```text
اولویت دوم
شروع نشده
نسخه اولیه توسط علی فقط در صورت داشتن زمان
```

## منطق تقسیم جدید ML

تقسیم جدید با هدف تکمیل امن و کامل مسئله ۱ انجام شده است.

تقسیم نهایی:

```text
علی: مسئله ۱، مقدمه و بخش ۱ ✅
بنیامین: مسئله ۱، بخش ۲ و بخش ۳
علی: شروع سبک مسئله ۲ فقط در صورت داشتن زمان
```

دلیل این تقسیم:

* مسئله ۱ چندبخشی و نمره‌دار است و باید کامل انجام شود.
* بخش علی شامل انتخاب ویژگی، اجرای KMeans با ۱۰ خوشه، تصویرسازی و تفسیر توصیه‌گر است.
* بخش بنیامین شامل انتخاب k با WCSS و DBSCAN است.
* برای متعادل‌تر شدن بار کاری، علی در صورت تکمیل بخش خودش می‌تواند نسخه baseline مسئله ۲ را شروع کند.
* کار علی و بنیامین در مسئله ۱ باید از نظر load data، فیلتر و featureهای پایه سازگار باشد.

## نکات اجرایی برای بخش ML

* همه کارهای مسئله ۱ باید از فایل `cleaned_step3_geo.parquet` شروع شوند.
* علی و بنیامین باید فیلتر پایه یکسان داشته باشند.
* علی و بنیامین باید در صورت sample گرفتن، از `random_state = 42` استفاده کنند.
* در بخش علی، sample فقط برای رسم scatter plot استفاده شده و مدل KMeans روی کل داده فیلترشده اجرا شده است.
* فایل‌های سنگین خروجی نباید وارد GitHub شوند.
* اگر برای سرعت اجرا از sample استفاده شد، باید روش نمونه‌گیری در نوت‌بوک توضیح داده شود.
* برای خوشه‌بندی مکانی، استفاده مستقیم از `utm_easting` و `utm_northing` بدون کنترل UTM zone مجاز نیست.
* برای مدل prediction، انتخاب مدل و هایپرپارامتر نباید با داده test انجام شود.
* داده test فقط یک بار برای گزارش نهایی استفاده می‌شود.
* در همه نوت‌بوک‌ها باید توضیح Markdown قبل از سلول‌های اصلی نوشته شود.
* Markdownهای توضیحی فارسی در نوت‌بوک‌ها بهتر است با قالب RTL نوشته شوند:

```html
<div dir="rtl" align="right">

متن توضیحی

</div>
```

## وضعیت فعلی پروژه

### preprocessing

```text
01_preprocessing.ipynb: کامل و commit شده ✅
```

آخرین خروجی آماده استفاده:

```text
/content/drive/MyDrive/divar_project/cleaned_step3_geo.parquet
```

### آمار توصیفی

```text
سؤال ۱: کامل شده ✅
سؤال ۲: کامل شده ✅
سؤال ۳: کامل شده ✅
سؤال ۴: کامل شده توسط بنیامین ✅
سؤال ۵: کامل شده توسط بنیامین ✅
سؤال ۶: کامل شده توسط بنیامین ✅
سؤال ۷: فعلاً انجام نشد
سؤال ۸ و ۹: مسئول تینا
```

### آزمون فرض

```text
فرضیه ۱: کامل شده و commit شده توسط علی ✅
فرضیه ۲: کامل شده توسط بنیامین ✅
```

### یادگیری ماشین

```text
مسئله ۱: اولویت اصلی، مسئول علی و بنیامین
مسئله ۱، مقدمه و بخش ۱: کامل شده توسط علی ✅
مسئله ۱، بخش ۲ و بخش ۳: مسئول بنیامین
مسئله ۲: اولویت دوم، شروع نشده
```
